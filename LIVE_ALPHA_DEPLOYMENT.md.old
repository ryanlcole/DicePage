# ReLiCGameMaster Live Alpha Deployment

Canonical repository:

`https://github.com/ryanlcole/DicePage.git`

Canonical tactical source:

`apps/tactical`

Current deployment status:

`repository_ready_azure_binding_pending`

Public build command:

```powershell
python deployment\build_public.py
python deployment\public_build_scan.py
```

Deployment gate:

No public Azure deployment should occur until the Azure target is positively identified, DNS changes are documented, public artifact scanning passes, and Owner approval is recorded for any paid or risky resource change.

