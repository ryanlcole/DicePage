(()=>{
 'use strict';
 const PREF_KEY='rist.gameStart.selection.v2';
 const SIGNUP_KEY='rist.signup.draft.v1';
 const WELCOME_KEY='rist.signup.welcomeSeen.v1';
 const AUTH_INTENT_KEY='rist.auth.intent';
 const VOICE_DEMO='Welcome to Shaelvien. Your journey begins here.';
 let root=null,view='splash';
 const parse=(v,f)=>{try{return v?JSON.parse(v):f}catch{return f}};
 const clean=v=>String(v||'').trim().slice(0,64);
 const savedPrefs=()=>{const p=parse(localStorage.getItem(PREF_KEY),{});return{role:p.role==='GameMaster'?'GameMaster':'Roleplayer',domain:p.domain==='RIST'?'RIST':'Shaelvien MMO'}};
 const waitFor=(test,timeout=7000)=>new Promise(resolve=>{const start=Date.now();const tick=()=>{let value=null;try{value=test()}catch{}if(value)return resolve(value);if(Date.now()-start>=timeout)return resolve(null);setTimeout(tick,80)};tick()});
 async function driveUntil(readState,wanted,buttonSelector,timeout=5000){const start=Date.now();while(Date.now()-start<timeout){if(readState()===wanted)return true;const button=document.querySelector(buttonSelector);if(button)button.click();await new Promise(r=>setTimeout(r,140));}return readState()===wanted}
 function shell(){return `<img class="rist-game-start-art" src="assets/branding/shaelvien-dragon-creation-startup-screen.png" alt="Primordial dragons forming Shaelvien with their breath weapons."><div class="rist-game-start-shade" aria-hidden="true"></div><main class="rist-game-start-panel" data-start-panel></main>`}
 function ensure(){if(root&&document.body.contains(root))return;root=document.createElement('section');root.className='rist-game-start';root.setAttribute('role','dialog');root.setAttribute('aria-modal','true');root.setAttribute('aria-label','Shaelvien start screen');root.innerHTML=shell();document.body.appendChild(root);root.addEventListener('click',onClick);root.addEventListener('submit',onSubmit);root.addEventListener('change',onChange)}
 function voiceOptions(){const voices=window.RistRoleplayVoice?.getVoices?.()||[];const current=window.RistRoleplayVoice?.getDefault?.();return `<div class="rist-start-voice-row"><label class="rist-start-voice">Default voice<select name="defaultVoice" aria-label="Default voice"><option value="">System default</option>${voices.map(v=>`<option value="${String(v.voiceURI||v.name).replace(/"/g,'&quot;')}" ${(current&&(current.voiceURI===v.voiceURI||current.name===v.name))?'selected':''}>${v.name}${v.lang?` (${v.lang})`:''}</option>`).join('')}</select></label><button type="button" class="rist-start-voice-demo" data-action="voice-demo" aria-label="Play selected voice demo" title="Play selected voice demo">▶</button></div>`}
 function playVoiceDemo(){const select=root?.querySelector('select[name="defaultVoice"]');const voiceId=select?.value||'';if(voiceId)window.RistRoleplayVoice?.setDefault?.(voiceId);const synth=window.speechSynthesis;if(!synth){window.RistRoleplayVoice?.speak?.({text:VOICE_DEMO,identity:'default'});return}const voices=synth.getVoices?.()||[];const voice=voiceId?voices.find(v=>v.voiceURI===voiceId||v.name===voiceId):voices.find(v=>v.default)||null;const utter=new SpeechSynthesisUtterance(VOICE_DEMO);if(voice){utter.voice=voice;utter.lang=voice.lang||'en-US'}synth.cancel();synth.speak(utter)}
 function setView(next){ensure();view=next;const panel=root.querySelector('[data-start-panel]');const p=savedPrefs();
  if(next==='splash')panel.innerHTML=`<button class="rist-start-anykey" type="button" data-action="continue">Press any key to continue…</button>`;
  if(next==='authwait')panel.innerHTML=`<div class="rist-start-authwait" role="status" aria-live="polite">Signing in…</div>`;
  if(next==='entry')panel.innerHTML=`<nav class="rist-start-menu" aria-label="Start"><button type="button" data-action="signin">Sign In</button><button type="button" data-action="signup">Sign Up</button><button type="button" data-action="preview">Preview</button></nav>`;
  if(next==='signin')panel.innerHTML=`<section class="rist-start-card"><h1>Sign In</h1><button type="button" data-action="discord-login">Discord Login</button><button class="rist-start-back" type="button" data-action="entry">Return</button></section>`;
  if(next==='signup')panel.innerHTML=`<form class="rist-start-card" data-signup-form><h1>Sign Up</h1><label>Handle<input name="handle" maxlength="64" autocomplete="nickname" required></label><label>Plan<input name="plan" maxlength="64" placeholder="Plan" required></label><label class="rist-start-agreement"><input type="checkbox" name="agreements" required><span>Agreements</span></label><button type="submit">Continue…</button><button class="rist-start-back" type="button" data-action="entry">Return</button></form>`;
  if(next==='welcome')panel.innerHTML=`<button class="rist-start-welcome" type="button" data-action="welcome-continue"><strong>Welcome to Shaelvien</strong><span>Click to continue…</span></button>`;
  if(next==='home')panel.innerHTML=`<nav class="rist-start-menu rist-start-home" aria-label="Campaign"><button type="button" data-action="new-campaign">New Campaign</button><button type="button" data-action="load-campaign">Load Campaign</button><button type="button" data-action="settings">Settings</button><button type="button" data-action="about">About</button></nav>`;
  if(next==='settings')panel.innerHTML=`<form class="rist-start-card" data-settings-form><h1>Settings</h1><fieldset><legend>Role</legend><label><input type="radio" name="role" value="GameMaster" ${p.role==='GameMaster'?'checked':''}><span>GameMaster</span></label><label><input type="radio" name="role" value="Roleplayer" ${p.role==='Roleplayer'?'checked':''}><span>Roleplayer</span></label></fieldset><fieldset><legend>Domain</legend><label><input type="radio" name="domain" value="Shaelvien MMO" ${p.domain==='Shaelvien MMO'?'checked':''}><span>Shaelvien MMO</span></label><label><input type="radio" name="domain" value="RIST" ${p.domain==='RIST'?'checked':''}><span>RIST</span></label></fieldset><fieldset><legend>Sound</legend>${voiceOptions()}</fieldset><button type="submit">Save</button><button class="rist-start-back" type="button" data-action="home">Return</button></form>`;
  if(next==='about')panel.innerHTML=`<section class="rist-start-card"><h1>About</h1><button type="button" data-action="about-open">About Shaelvien / RIST</button><button class="rist-start-back" type="button" data-action="home">Return</button></section>`;
  if(next!=='authwait')requestAnimationFrame(()=>panel.querySelector('button,input,select')?.focus())
 }
 async function discordAuth(intent){localStorage.setItem(AUTH_INTENT_KEY,intent);sessionStorage.removeItem('rist-public-preview');delete document.documentElement.dataset.ristPublicPreview;try{const cfg=await fetch('auth-config.json',{cache:'no-store'}).then(r=>r.json());if(cfg?.apiBaseUrl){const next=encodeURIComponent(location.href.split('#')[0]);const path=String(cfg.loginPath||'/auth/login').startsWith('/')?String(cfg.loginPath||'/auth/login'):`/${cfg.loginPath}`;location.href=`${cfg.apiBaseUrl.replace(/\/$/,'')}${path}?next=${next}&intent=${encodeURIComponent(intent)}`;return}}catch{}document.dispatchEvent(new CustomEvent('rist:auth-request',{detail:{intent}}))}
 function persistPrefs(p){localStorage.setItem(PREF_KEY,JSON.stringify(p));localStorage.setItem('rist.topFrame.userRole',p.role);localStorage.setItem('rist.topFrame.domain',p.domain);document.dispatchEvent(new CustomEvent('rist:start-settings-changed',{detail:p}))}
 async function applyPrefs(){const p=savedPrefs();await waitFor(()=>document.querySelector('.mmo-zone-actions[data-world-mode][data-world-role]'),7000);const mode=()=>document.querySelector('.mmo-zone-actions[data-world-mode]')?.dataset.worldMode||document.querySelector('.map-shell[data-world-mode]')?.dataset.worldMode||'';const wantedMode=p.domain==='RIST'?'sandbox':'mmo';await driveUntil(mode,wantedMode,wantedMode==='sandbox'?'.mmo-mode-sandbox-action':'.mmo-mode-shaelvien-action');const roleState=()=>document.querySelector('.mmo-zone-actions[data-world-role]')?.dataset.worldRole||'';const wantedRole=p.role==='GameMaster'?'GM':'PC';await driveUntil(roleState,wantedRole,p.role==='GameMaster'?'.mmo-role-gm-action':'.mmo-role-player-action');document.dispatchEvent(new CustomEvent('rist:start-settings-applied',{detail:{...p,appliedRole:roleState(),appliedMode:mode()}}));return p}
 async function launch(kind){const p=await applyPrefs();sessionStorage.setItem('rist.gameStart.current',JSON.stringify({...p,kind}));document.dispatchEvent(new CustomEvent(kind==='load'?'rist:load-campaign':'rist:new-campaign',{detail:p}));document.dispatchEvent(new CustomEvent('rist:game-start',{detail:{...p,kind}}));close()}
 function preview(){document.dispatchEvent(new CustomEvent('rist:preview-request'));close()}
 function onClick(e){const b=e.target.closest?.('[data-action]');if(!b)return;const a=b.dataset.action;if(a==='continue'||a==='entry')setView('entry');else if(a==='signin')setView('signin');else if(a==='signup')setView('signup');else if(a==='preview')preview();else if(a==='discord-login')discordAuth('login');else if(a==='welcome-continue'){localStorage.setItem(WELCOME_KEY,'1');setView('home')}else if(a==='new-campaign')launch('new');else if(a==='load-campaign')launch('load');else if(a==='settings')setView('settings');else if(a==='about')setView('about');else if(a==='about-open')location.href='info.html';else if(a==='voice-demo')playVoiceDemo();else if(a==='home')setView('home')}
 function onChange(e){if(!e.target.closest('[data-settings-form]'))return;if(e.target.name==='defaultVoice'){if(e.target.value)window.RistRoleplayVoice?.setDefault?.(e.target.value);playVoiceDemo()}}
 async function onSubmit(e){if(e.target.matches('[data-signup-form]')){e.preventDefault();const data=new FormData(e.target);const draft={handle:clean(data.get('handle')),plan:clean(data.get('plan')),agreements:data.get('agreements')==='on'};if(!draft.handle||!draft.plan||!draft.agreements)return;localStorage.setItem(SIGNUP_KEY,JSON.stringify(draft));localStorage.setItem(AUTH_INTENT_KEY,'signup');document.dispatchEvent(new CustomEvent('rist:signup-draft',{detail:draft}));discordAuth('signup');return}if(e.target.matches('[data-settings-form]')){e.preventDefault();const data=new FormData(e.target);const p={role:String(data.get('role')||'Roleplayer'),domain:String(data.get('domain')||'Shaelvien MMO')};persistPrefs(p);if(data.get('defaultVoice'))window.RistRoleplayVoice?.setDefault?.(String(data.get('defaultVoice')));await applyPrefs();setView('home')}}
 function open(options={}){ensure();root.hidden=false;document.body.classList.add('rist-game-start-open');if(options.authenticated||options.fromGame){if(options.view)setView(options.view);else{const intent=localStorage.getItem(AUTH_INTENT_KEY)||'login';if(intent==='signup'&&localStorage.getItem(WELCOME_KEY)!=='1')setView('welcome');else setView('home')}}else setView(options.view||'splash')}
 function close(){if(!root)return;root.hidden=true;document.body.classList.remove('rist-game-start-open');window.speechSynthesis?.cancel?.()}
 addEventListener('keydown',e=>{if(root&&!root.hidden&&view==='splash'){e.preventDefault();setView('entry')}else if(e.key==='Escape'&&root&&!root.hidden&&view!=='authwait'){if(view==='settings'||view==='about')setView('home');else if(view!=='entry'&&view!=='home')setView('entry')}});
 addEventListener('pointerdown',e=>{if(root&&!root.hidden&&view==='splash'&&!e.target.closest('button'))setView('entry')});
 window.RistGameStartScreen={open,close,setView,getSelection:savedPrefs,applyPrefs};
})();
