(()=>{
 'use strict';
 const ZONE_KEY='rist:mmo-zone';
 let corner='';
 let zone=(()=>{try{return JSON.parse(localStorage.getItem(ZONE_KEY)||'null')}catch{return null}})();
 function getOverlay(){let overlay=document.querySelector('.mmo-build-overlay');if(overlay)return overlay;overlay=document.createElement('div');overlay.className='mmo-build-overlay';overlay.hidden=true;overlay.innerHTML='<section class="mmo-build-panel" role="dialog" aria-modal="true" aria-label="Shaelvien world expansion"></section>';document.body.appendChild(overlay);overlay.addEventListener('click',e=>{if(e.target===overlay)close()});return overlay}
 const panel=()=>getOverlay().querySelector('.mmo-build-panel');
 const close=()=>{getOverlay().hidden=true};
 function saveZone(next){zone=next;localStorage.setItem(ZONE_KEY,JSON.stringify(next));syncState()}
 const hasWorldExpansionPublic=()=>[...document.querySelectorAll('.public-table-card .public-card-name')].some(node=>node.textContent.trim().toLowerCase()==='world expansion');
 function syncState(){const claimed=zone?.type==='shaelvien',expansion=hasWorldExpansionPublic();document.body.classList.toggle('rist-mmo-zone-claimed',claimed);document.body.classList.toggle('rist-mmo-owned-zone',claimed);document.body.classList.toggle('rist-world-expansion-public',expansion)}
 function first(){const p=panel();p.innerHTML='<h2>Are you a GameMaster?</h2><div class="mmo-choice-row"><button data-a="yes">Yes</button><button data-a="no">No</button><button data-a="explain">Explain…</button></div><button class="mmo-close" data-close>Exit</button>';getOverlay().hidden=false;p.onclick=e=>{const answer=e.target.closest('[data-a]')?.dataset.a;if(answer==='yes')second();else if(answer==='no'||e.target.closest('[data-close]'))close();else if(answer==='explain')explain()}}
 function explain(){const p=panel();p.innerHTML='<h2>GameMaster Expansion</h2><p>Your first Personal zone is a blank map you build from scratch. Your first Shaelvien zone receives the current MMO map as its starting map. After a zone is claimed, adjacent + controls remain hidden unless a purchased World Expansion card is flipped to the public card area.</p><p>Shaelvien grows like a chess board. AI connective building only becomes eligible after two GM maps connect to the same blank area.</p><button data-back>Back</button><button class="mmo-close" data-close>Exit</button>';p.onclick=e=>{if(e.target.closest('[data-back]'))first();else if(e.target.closest('[data-close]'))close()}}
 function second(){const p=panel();p.innerHTML='<h2>How will you build?</h2><div class="mmo-choice-row"><button data-build="personal">Personal</button><button data-build="shaelvien">Shaelvien</button></div><button class="mmo-close" data-close>Exit</button>';p.onclick=e=>{const type=e.target.closest('[data-build]')?.dataset.build;if(type)choose(type);else if(e.target.closest('[data-close]'))close()}}
 function choose(type){
  if(type==='personal'){window.RistMMO?.setMode?.('local');document.querySelector('.mmo-personal-zone-action')?.click();const record={type:'personal',corner:null,claimed:true,blank:true};saveZone(record);close();document.dispatchEvent(new CustomEvent('rist:mmo-build',{detail:record}));return}
  window.RistMMO?.setMode?.('shaelvien');document.querySelector('.mmo-shaelvien-zone-action')?.click();const record={type:'shaelvien',corner,claimed:true,status:'owned-zone',connections:1,aiEligible:false};saveZone(record);const p=panel();p.innerHTML='<h2>Shaelvien Zone Claimed</h2><p>The current map is now the starting map for your GM zone. This zone is editable by its owner. World expansion controls are hidden until a World Expansion card is purchased and flipped public.</p><p><strong>Connections:</strong> 1 / 2<br><strong>AI white-space:</strong> Locked</p><button class="mmo-close" data-close>Exit</button>';p.onclick=e=>{if(e.target.closest('[data-close]'))close()};document.dispatchEvent(new CustomEvent('rist:mmo-build',{detail:record}));
 }
 function ensure(){document.querySelectorAll('.map-shell').forEach(shell=>{if(shell.querySelector('.mmo-corner-plus'))return;for(const c of ['nw','ne','sw','se']){const button=document.createElement('button');button.type='button';button.className=`mmo-corner-plus mmo-${c}`;button.textContent='+';button.setAttribute('aria-label','Build beside map');button.onclick=e=>{e.stopPropagation();corner=c;first()};shell.appendChild(button)}});syncState()}
 const queue=()=>window.RistRuntime?.frame?.('mmo-build',ensure)??requestAnimationFrame(ensure);
 document.addEventListener('rist:dom-change',queue);document.addEventListener('rist:mmo-mode',syncState);queue();
})();
