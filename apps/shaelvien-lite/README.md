# Shaelvien Lite Transitional Location

Shaelvien Lite remains in the recovered repository root for this live-alpha import pass.

Canonical current path:

`C:\ReLiC\projects\DicePage\shaelvien_lite`

The code was not moved during `RELIC-GAMEMASTER-LIVE-ALPHA-0` because the Owner directive required preserving behavior and running the existing test suite. This directory is a repository-layout marker so future restructuring can move Shaelvien Lite intentionally instead of creating a duplicate active copy.

