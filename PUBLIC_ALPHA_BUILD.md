# Shaelvien Public Alpha

Target path: Enter -> Build -> Zoom -> Place -> Play -> Replay -> Save -> Feedback.

Public-alpha shell requirements:
- Preserve the existing RIST world as implementation authority.
- Present a cinematic Shaelvien entry before the world surface.
- Keep the existing Discord/account/terms gate intact.
- Keep map, assets, recursion, character, dice, chat, and persistence systems behind the entry rather than duplicating them.
- Fit inside a 100dvh viewport; the alpha status ribbon consumes 30px and the world context strip consumes 42px.
- Mobile and reduced-motion behavior must remain usable.

Recovery authority: backup/pre-public-alpha-2026-09-08.
