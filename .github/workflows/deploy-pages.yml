name: Legacy Pages Deploy Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain canonical deployment
        run: |
          echo "Legacy Pages deployment is disabled."
          echo "RIST WORLD deploys only from live-alpha-rist-blazor-world via deploy-live-alpha-public.yml."
